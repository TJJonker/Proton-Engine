#include "ppch.h"
#include "Rendering/Texture/Texture.h"

namespace Proton::Rendering {
    Texture* Texture::Create(U16 width, U16 height, TextureChannels channels, const void* data, TextureConfigs configs)
    {
        return nullptr;
    }
}

#include "ppch.h"
#include "Rendering/VertexBuffer/VertexBuffer.h"

namespace Proton::Rendering {
	VertexBuffer* VertexBuffer::Create(const void* vertices, U32 size)
	{
		ASSERT(false);
		return nullptr;
	}
}